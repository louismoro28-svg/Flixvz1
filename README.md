# Flixvz1

A video streaming platform application.

## Setup

To configure the repository with the correct remote and push to the main branch:

```bash
git remote add origin https://github.com/louismoro28-svg/FlixV.git
git branch -M main
git push -u origin main
```

## Getting Started

More documentation coming soon.